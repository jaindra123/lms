<?php
/**
 * PRODUCTION secrets — copy this file to  config.production.php  on the
 * production server ONLY, then fill in the real values.
 *
 *   cp config.production.php.example config.production.php
 *
 * config.production.php is git-ignored and must never be committed.
 * $CFG is already initialised by config.php before this file is loaded.
 */

// Public site URL (no trailing slash).
$CFG->wwwroot = 'https://lms.iiidem.in';

// Database connection.
$CFG->dbhost = '127.0.0.1';
$CFG->dbname = 'iiidem_prod';
$CFG->dbuser = 'iiidem_prod';
$CFG->dbpass = 'CHANGE_ME_PRODUCTION_DB_PASSWORD';

// Moodle data directory for this environment.
$CFG->dataroot = '/var/moodledata_prod';
